const getData = require('./getData.util');
const { breakByNewLines, splitByParameter } = require('./string.util');
const { sum } = require('./num.util');

module.exports = {
  getData,
  breakByNewLines,
  splitByParameter,
  sum,
};
